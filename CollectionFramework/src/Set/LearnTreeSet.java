package Set;

import java.util.TreeSet;
import java.util.Set;

public class LearnTreeSet {
    Set<Integer> set = new TreeSet<>();
    // In treeset
    // 1. No Duplicates
    // 2. Order is maintained
    // 3. Sorted by default(binary search tree(BST) data structure is used)
    // 4. Due to BST every operation takes O(logN) time

}
